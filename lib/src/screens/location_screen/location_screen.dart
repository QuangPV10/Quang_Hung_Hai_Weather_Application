import 'package:flutter/material.dart';

class Locationcreen extends StatelessWidget {
  const Locationcreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
